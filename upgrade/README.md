# Upgrade Scripts

This directory holds migration scripts for MAJOR version boundaries.

At v1.0.0, no upgrade scripts are needed. When v2.0.0 is released (planned: split bug log into one file per bug), a migration script will be added here to transform the single-file log into the per-bug format.
